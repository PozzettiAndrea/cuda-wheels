# Patched Sources

This branch contains the source code for each package after patches are applied.
It is maintained automatically by GitHub Actions — do not push to this branch manually.

Each subdirectory corresponds to a package in `packages/*.yml`.
A `.cuda-wheels-meta.json` file in each directory records the source repo, tag, and patch status.
